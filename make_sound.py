from matplotlib import pyplot as plt
# from func import *
import soundfile as sf
import sounddevice as sd
import wavio



# fs = 44100
# a0, a1 = 200, 200
# f, f1 = 1, 10
# dt = 0.01
# N = 100000
# data = ([(a0 * sin(2 * np.pi * f * k * dt)) for k in range(N)])
#
# data1 = ([(a1 * sin(2 * np.pi * f1 * k * dt)) for k in range(N)])
#
# data3 = data + data1
#
# sf.write("myfile.wav", data, fs)

